const HTML = `<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Who's The Smartest? - Brain Training Game</title>
    <meta name="description" content="Test your brain with 4 mini-games!">
    <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🧠</text></svg>">
    <script crossorigin src="https://unpkg.com/react@18/umd/react.production.min.js"><\/script>
    <script crossorigin src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"><\/script>
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"><\/script>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Segoe UI', system-ui, sans-serif; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); min-height: 100vh; display: flex; justify-content: center; align-items: center; padding: 20px; }
        .brain-game { width: 100%; max-width: 800px; min-height: 600px; background: white; border-radius: 20px; box-shadow: 0 20px 60px rgba(0,0,0,0.3); padding: 40px; position: relative; }
        .menu, .results { text-align: center; }
        h1 { font-size: 2.5rem; color: #667eea; margin-bottom: 20px; }
        .subtitle { font-size: 1.2rem; color: #666; margin-bottom: 30px; }
        button { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; border: none; padding: 15px 40px; font-size: 1.2rem; border-radius: 50px; cursor: pointer; transition: transform 0.2s, box-shadow 0.2s; margin: 10px; }
        button:hover { transform: translateY(-2px); box-shadow: 0 10px 20px rgba(0,0,0,0.2); }
        button:disabled { opacity: 0.5; cursor: not-allowed; }
        .btn-secondary { background: #f0f0f0; color: #333; }
        .mini-game { animation: fadeIn 0.5s; }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }
        .game-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 30px; padding-bottom: 20px; border-bottom: 2px solid #eee; flex-wrap: wrap; gap: 10px; }
        .game-header h2 { font-size: 1.8rem; color: #667eea; }
        .stats { display: flex; gap: 20px; font-size: 1.1rem; font-weight: bold; color: #555; }
        .block-container, .cards-grid, .math-problem, .instruction { display: flex; flex-direction: column; align-items: center; justify-content: center; margin: 30px 0; }
        .cards-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 12px; max-width: 400px; margin: 0 auto; }
        .card { aspect-ratio: 1; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); border-radius: 10px; display: flex; align-items: center; justify-content: center; font-size: 2rem; color: white; cursor: pointer; transition: transform 0.3s, background 0.3s; user-select: none; min-width: 60px; min-height: 60px; }
        .card:not(.revealed) { background: #ddd; color: #ddd; }
        .card:hover:not(.revealed):not(.matched) { transform: scale(1.05); background: #ccc; }
        .card.matched { background: #4caf50; }
        .input-area { display: flex; gap: 15px; align-items: center; justify-content: center; margin-top: 30px; flex-wrap: wrap; }
        input[type="number"], input[type="text"] { padding: 15px 25px; font-size: 1.5rem; border: 3px solid #ddd; border-radius: 10px; width: 200px; text-align: center; }
        input:focus { outline: none; border-color: #667eea; }
        input.correct { border-color: #4caf50; background: #e8f5e9; }
        input.incorrect { border-color: #f44336; animation: shake 0.5s; }
        @keyframes shake { 0%, 100% { transform: translateX(0); } 25% { transform: translateX(-10px); } 75% { transform: translateX(10px); } }
        .question { font-size: 3rem; font-weight: bold; color: #333; margin-bottom: 20px; }
        .instruction { font-size: 1.2rem; color: #555; display: flex; align-items: center; gap: 10px; margin-bottom: 20px; flex-wrap: wrap; justify-content: center; }
        .results-card { background: #f8f9fa; border-radius: 15px; padding: 30px; margin: 20px 0; }
        .brain-size { font-size: 3.5rem; color: #667eea; font-weight: bold; margin: 15px 0; }
        .brain-label { font-size: 1.8rem; color: #764ba2; margin-bottom: 20px; }
        .score-breakdown { text-align: left; margin-top: 20px; }
        .score-item { display: flex; justify-content: space-between; padding: 12px 15px; margin: 8px 0; background: white; border-radius: 10px; }
        .progress-bar { width: 100%; height: 8px; background: #eee; border-radius: 10px; margin: 20px 0; overflow: hidden; }
        .progress-fill { height: 100%; background: linear-gradient(90deg, #667eea 0%, #764ba2 100%); transition: width 0.3s; }
        .modal-overlay { position: fixed; top: 0; left: 0; right: 0; bottom: 0; background: rgba(0,0,0,0.7); display: flex; align-items: center; justify-content: center; z-index: 1000; }
        .modal { background: white; border-radius: 20px; padding: 40px; max-width: 500px; width: 90%; text-align: center; }
        .modal h2 { color: #667eea; margin-bottom: 20px; }
        .leaderboard { margin-top: 30px; }
        .leaderboard h3 { color: #667eea; margin-bottom: 15px; font-size: 1.5rem; }
        .leaderboard-list { max-height: 300px; overflow-y: auto; }
        .leaderboard-item { display: flex; justify-content: space-between; align-items: center; padding: 12px 15px; margin: 5px 0; background: #f8f9fa; border-radius: 10px; }
        .leaderboard-item.highlight { background: linear-gradient(135deg, #667eea22 0%, #764ba222 100%); border: 2px solid #667eea; }
        .leaderboard-rank { font-weight: bold; color: #667eea; width: 40px; }
        .leaderboard-name { flex: 1; text-align: left; margin-left: 10px; }
        .leaderboard-score { font-weight: bold; color: #764ba2; }
        .leaderboard-badge { font-size: 1.2rem; margin-left: 10px; }
        .menu-buttons { display: flex; flex-direction: column; gap: 15px; align-items: center; margin-top: 30px; }
        @media (max-width: 600px) { .brain-game { padding: 20px; } h1 { font-size: 1.8rem; } .question { font-size: 2rem; } .brain-size { font-size: 2.5rem; } .card { font-size: 1.5rem; min-width: 50px; min-height: 50px; } button { padding: 12px 30px; font-size: 1rem; } input { font-size: 1.2rem; padding: 12px 20px; width: 150px; } }
    </style>
</head>
<body>
    <div id="root"></div>
    <script type="text/babel">
        const { useState, useEffect, useCallback } = React;
        const API = { async getLeaderboard() { try { const res = await fetch('/api/leaderboard'); const data = await res.json(); return data.success ? data.leaderboard : []; } catch { return []; } }, async submitScore(name, score, brainSize, brainLabel) { try { const res = await fetch('/api/score', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ name, score, brainSize, brainLabel }) }); return await res.json(); } catch { return { success: false }; } } };
        const BlockCountingGame = ({ onComplete, difficulty }) => { const [timeLeft, setTimeLeft] = useState(30); const [score, setScore] = useState(0); const [blocks, setBlocks] = useState([]); const [answer, setAnswer] = useState(''); const [feedback, setFeedback] = useState(null); const gen = useCallback(() => { const n = Math.floor(5 + difficulty * 10 + Math.random() * 10); const b = []; for (let i = 0; i < n; i++) b.push({ x: Math.floor(Math.random() * 5), y: Math.floor(Math.random() * 3), z: Math.floor(Math.random() * 5) }); setBlocks(b); setAnswer(''); setFeedback(null); }, [difficulty]); useEffect(gen, [gen]); useEffect(() => { if (timeLeft <= 0) { onComplete(score); return; } const t = setTimeout(() => setTimeLeft(x => x - 1), 1000); return () => clearTimeout(t); }, [timeLeft, score, onComplete]); const submit = () => { if (parseInt(answer) === blocks.length) { setFeedback('correct'); setScore(s => s + 100); setTimeout(gen, 500); } else { setFeedback('incorrect'); setTimeout(() => setFeedback(null), 500); } }; return (<div className="mini-game"><div className="game-header"><h2>🧊 Block Counting</h2><div className="stats"><span>⏱️ {timeLeft}s</span><span>⭐ {score}</span></div></div><div className="block-container"><svg width="400" height="300" viewBox="0 0 400 300">{blocks.map((b, i) => { const x = 200 + (b.x - b.z) * 20, y = 150 + (b.x + b.z) * 10 - b.y * 30; return <g key={i}><polygon points={x+","+y+" "+(x+20)+","+(y+10)+" "+x+","+(y+20)+" "+(x-20)+","+(y+10)} fill="#4a90e2" stroke="#2c5aa0"/><polygon points={x+","+y+" "+(x-20)+","+(y+10)+" "+(x-20)+","+(y+40)+" "+x+","+(y+30)} fill="#357abd" stroke="#2c5aa0"/><polygon points={x+","+y+" "+(x+20)+","+(y+10)+" "+(x+20)+","+(y+40)+" "+x+","+(y+30)} fill="#5ba3e8" stroke="#2c5aa0"/></g>; })}</svg></div><div className="input-area"><input type="number" value={answer} onChange={e => setAnswer(e.target.value)} onKeyPress={e => e.key === 'Enter' && submit()} placeholder="How many?" className={feedback||''} autoFocus/><button onClick={submit}>Submit</button></div></div>); };
        const MemoryCardsGame = ({ onComplete, difficulty }) => { const [timeLeft, setTimeLeft] = useState(45); const [score, setScore] = useState(0); const [phase, setPhase] = useState('memorize'); const [cards, setCards] = useState([]); const [revealed, setRevealed] = useState([]); const [selected, setSelected] = useState([]); const [memTime, setMemTime] = useState(4); const syms = ['🍎','🍊','🍋','🍇','🍓','🍒','🥝','🍑']; const gen = useCallback(() => { const s = [...syms].sort(() => Math.random() - 0.5).slice(0, 4); setCards([...s, ...s].sort(() => Math.random() - 0.5)); setRevealed([]); setSelected([]); setPhase('memorize'); setMemTime(4); }, []); useEffect(gen, [gen]); useEffect(() => { if (timeLeft <= 0) { onComplete(score); return; } const t = setTimeout(() => setTimeLeft(x => x - 1), 1000); return () => clearTimeout(t); }, [timeLeft, score, onComplete]); useEffect(() => { if (phase === 'memorize') { if (memTime > 0) { const t = setTimeout(() => setMemTime(x => x - 1), 1000); return () => clearTimeout(t); } else setPhase('recall'); } }, [phase, memTime]); useEffect(() => { if (selected.length === 2) { const [a, b] = selected; if (cards[a] === cards[b]) { setRevealed(r => [...r, a, b]); setScore(s => s + 100); setSelected([]); if (revealed.length + 2 === cards.length) setTimeout(gen, 1000); } else setTimeout(() => setSelected([]), 600); } }, [selected, cards, revealed, gen]); const click = i => { if (phase === 'memorize' || revealed.includes(i) || selected.includes(i) || selected.length >= 2) return; setSelected(s => [...s, i]); }; return (<div className="mini-game"><div className="game-header"><h2>🃏 Memory Cards</h2><div className="stats"><span>⏱️ {timeLeft}s</span><span>⭐ {score}</span>{phase === 'memorize' && <span>👀 {memTime}s</span>}</div></div><p className="instruction">{phase === 'memorize' ? 'Memorize the cards!' : 'Find the matching pairs!'}</p><div className="cards-grid">{cards.map((sym, i) => { const show = phase === 'memorize' || revealed.includes(i) || selected.includes(i); return <div key={i} className={"card " + (show ? "revealed " : "") + (revealed.includes(i) ? "matched" : "")} onClick={() => click(i)}>{show ? sym : '?'}</div>; })}</div></div>); };
        const MathGame = ({ onComplete, difficulty }) => { const [timeLeft, setTimeLeft] = useState(40); const [score, setScore] = useState(0); const [prob, setProb] = useState({ q: '', a: 0 }); const [ans, setAns] = useState(''); const [feedback, setFeedback] = useState(null); const gen = useCallback(() => { const ops = ['+', '-', '×']; const op = ops[Math.floor(Math.random() * 3)]; const a = Math.floor(2 + Math.random() * 20), b = Math.floor(2 + Math.random() * 15); let c; if (op === '+') c = a + b; else if (op === '-') c = a - b; else c = a * b; setProb({ q: a + ' ' + op + ' ' + b, a: c }); setAns(''); setFeedback(null); }, []); useEffect(gen, [gen]); useEffect(() => { if (timeLeft <= 0) { onComplete(score); return; } const t = setTimeout(() => setTimeLeft(x => x - 1), 1000); return () => clearTimeout(t); }, [timeLeft, score, onComplete]); const submit = () => { if (parseInt(ans) === prob.a) { setFeedback('correct'); setScore(s => s + 75); setTimeout(gen, 400); } else { setFeedback('incorrect'); setTimeout(() => setFeedback(null), 400); } }; return (<div className="mini-game"><div className="game-header"><h2>🔢 Quick Math</h2><div className="stats"><span>⏱️ {timeLeft}s</span><span>⭐ {score}</span></div></div><div className="math-problem"><div className="question">{prob.q} = ?</div></div><div className="input-area"><input type="number" value={ans} onChange={e => setAns(e.target.value)} onKeyPress={e => e.key === 'Enter' && submit()} className={feedback||''} autoFocus/><button onClick={submit}>Submit</button></div></div>); };
        const AsteroidGame = ({ onComplete, difficulty }) => { const [timeLeft, setTimeLeft] = useState(35); const [score, setScore] = useState(0); const [asteroids, setAsteroids] = useState([]); const [target, setTarget] = useState(0); const gen = useCallback(() => { const sizes = Array.from({length: 8}, () => 20 + Math.random() * 50); setTarget(sizes[Math.floor(Math.random() * sizes.length)]); setAsteroids(sizes.map((s, i) => ({ size: s, x: 30 + Math.random() * 340, y: 30 + Math.random() * 240, vx: (Math.random() - 0.5) * 1.5, vy: (Math.random() - 0.5) * 1.5, id: i }))); }, []); useEffect(gen, [gen]); useEffect(() => { if (timeLeft <= 0) { onComplete(score); return; } const t = setTimeout(() => setTimeLeft(x => x - 1), 1000); return () => clearTimeout(t); }, [timeLeft, score, onComplete]); useEffect(() => { const i = setInterval(() => setAsteroids(a => a.map(x => ({ ...x, x: ((x.x + x.vx) + 400) % 400, y: ((x.y + x.vy) + 300) % 300 }))), 50); return () => clearInterval(i); }, []); const click = a => { if (Math.abs(a.size - target) < 0.5) { setScore(s => s + 100); setTimeout(gen, 500); } }; return (<div className="mini-game"><div className="game-header"><h2>☄️ Asteroid Match</h2><div className="stats"><span>⏱️ {timeLeft}s</span><span>⭐ {score}</span></div></div><div className="instruction"><span>Click the asteroid matching this size:</span><svg width="70" height="70"><circle cx="35" cy="35" r={target/2} fill="#666" stroke="#fff" strokeWidth="2"/></svg></div><svg width="400" height="300" style={{background:'linear-gradient(to bottom,#0a0a2e,#1a1a4e)',borderRadius:'15px',display:'block',margin:'0 auto'}}>{asteroids.map(a => <circle key={a.id} cx={a.x} cy={a.y} r={a.size/2} fill="#666" stroke="#888" strokeWidth="2" onClick={() => click(a)} style={{cursor:'pointer'}}/>)}</svg></div>); };
        const NameModal = ({ onSubmit, score, brainLabel }) => { const [name, setName] = useState(localStorage.getItem('playerName') || ''); const [sub, setSub] = useState(false); const submit = async () => { if (!name.trim() || sub) return; setSub(true); localStorage.setItem('playerName', name.trim()); await onSubmit(name.trim()); }; return (<div className="modal-overlay"><div className="modal"><h2>🎉 Great Job!</h2><p style={{fontSize:'1.2rem',margin:'15px 0'}}>You scored <strong style={{color:'#667eea'}}>{score}</strong> points!</p><p style={{fontSize:'1.5rem',margin:'10px 0'}}>{brainLabel}</p><p style={{color:'#666',marginTop:'20px'}}>Enter your name for the leaderboard:</p><input type="text" value={name} onChange={e => setName(e.target.value)} onKeyPress={e => e.key === 'Enter' && submit()} placeholder="Your name" maxLength={20} autoFocus style={{width:'250px'}}/><div style={{marginTop:'20px'}}><button onClick={submit} disabled={!name.trim() || sub}>{sub ? 'Saving...' : 'Submit Score'}</button></div></div></div>); };
        const Leaderboard = ({ currentPlayerId }) => { const [lb, setLb] = useState([]); const [loading, setLoading] = useState(true); useEffect(() => { API.getLeaderboard().then(d => { setLb(d); setLoading(false); }); }, []); const emoji = r => r === 1 ? '🥇' : r === 2 ? '🥈' : r === 3 ? '🥉' : ''; if (loading) return <div className="leaderboard"><p>Loading...</p></div>; return (<div className="leaderboard"><h3>🏆 Leaderboard</h3><div className="leaderboard-list">{lb.slice(0, 10).map((e, i) => <div key={e.id} className={"leaderboard-item " + (e.id === currentPlayerId ? "highlight" : "")}><span className="leaderboard-rank">{emoji(i+1)} #{i+1}</span><span className="leaderboard-name">{e.name}</span><span className="leaderboard-score">{e.score}</span><span className="leaderboard-badge">{e.brainLabel?.includes('Space') ? '👽' : '🧠'}</span></div>)}{lb.length === 0 && <p style={{textAlign:'center',color:'#666',padding:'20px'}}>No scores yet!</p>}</div></div>); };
        const BrainGame = () => { const [state, setState] = useState('menu'); const [game, setGame] = useState(0); const [scores, setScores] = useState([]); const [total, setTotal] = useState(0); const [showModal, setShowModal] = useState(false); const [playerId, setPlayerId] = useState(null); const [rank, setRank] = useState(null); const games = [{id:'blocks',name:'Block Counting',cat:'Analysis',C:BlockCountingGame},{id:'memory',name:'Memory Cards',cat:'Memory',C:MemoryCardsGame},{id:'math',name:'Quick Math',cat:'Calculation',C:MathGame},{id:'asteroids',name:'Asteroid Match',cat:'Visual',C:AsteroidGame}]; const start = () => { setState('playing'); setGame(0); setScores([]); setTotal(0); setPlayerId(null); setRank(null); }; const done = s => { const g = games[game]; setScores(p => [...p, {cat:g.cat,name:g.name,score:s}]); setTotal(t => t + s); if (game < games.length - 1) setGame(g => g + 1); else { setState('results'); setShowModal(true); } }; const brain = s => { if (s < 400) return {size:1200,label:'Tiny Brain 🐜'}; if (s < 800) return {size:1400,label:'Small Brain 🐭'}; if (s < 1200) return {size:1600,label:'Average Brain 🧑'}; if (s < 1600) return {size:1800,label:'Smart Brain 🎓'}; if (s < 2000) return {size:2000,label:'Genius Brain 🧠'}; if (s < 2500) return {size:2200,label:'Einstein Brain 👨‍🔬'}; return {size:2500,label:'Space Alien 👽'}; }; const submitName = async n => { const b = brain(total); const r = await API.submitScore(n, total, b.size, b.label); if (r.success) { setPlayerId(r.entry.id); setRank(r.rank); } setShowModal(false); }; const G = games[game]?.C; const bi = brain(total); return (<div className="brain-game">{state === 'menu' && <div className="menu"><h1>🧠 Who's The Smartest?</h1><p className="subtitle">Test your brain in 4 categories!</p><div className="menu-buttons"><button onClick={start}>🎮 Play Now</button></div><Leaderboard/></div>}{state === 'playing' && G && <><div className="progress-bar"><div className="progress-fill" style={{width:(game/games.length*100)+'%'}}/></div><p style={{textAlign:'center',color:'#666',marginBottom:'10px'}}>Game {game+1} of {games.length} • {games[game].cat}</p><G onComplete={done} difficulty={0.5}/></>}{state === 'results' && <div className="results">{showModal && <NameModal onSubmit={submitName} score={total} brainLabel={bi.label}/>}<h1>🎉 Results!</h1><div className="results-card"><div className="brain-size">{bi.size} cm³</div><div className="brain-label">{bi.label}</div>{rank && <p style={{color:'#667eea',fontWeight:'bold'}}>🏆 Rank #{rank}!</p>}<div className="score-breakdown"><h3 style={{marginBottom:'15px'}}>Score Breakdown:</h3>{scores.map((s,i) => <div key={i} className="score-item"><span>{s.name}</span><span style={{fontWeight:'bold',color:'#667eea'}}>{s.score}</span></div>)}<div className="score-item" style={{background:'linear-gradient(135deg,#667eea,#764ba2)',color:'white',fontWeight:'bold',fontSize:'1.2rem',marginTop:'15px'}}><span>Total</span><span>{total}</span></div></div></div><button onClick={start}>🔄 Play Again</button><button className="btn-secondary" onClick={() => setState('menu')}>🏠 Menu</button><Leaderboard currentPlayerId={playerId}/></div>}</div>); };
        ReactDOM.render(<BrainGame/>, document.getElementById('root'));
    <\/script>
</body>
</html>`;

export default {
  async fetch(request, env) {
    const url = new URL(request.url);
    if (url.pathname === '/api/leaderboard' && request.method === 'GET') {
      try {
        const data = await env.LEADERBOARD.get('leaderboard_v1', { type: 'json' });
        return new Response(JSON.stringify({ success: true, leaderboard: data || [] }), { headers: { 'Content-Type': 'application/json' } });
      } catch { return new Response(JSON.stringify({ success: true, leaderboard: [] }), { headers: { 'Content-Type': 'application/json' } }); }
    }
    if (url.pathname === '/api/score' && request.method === 'POST') {
      try {
        const { name, score, brainSize, brainLabel } = await request.json();
        if (!name || typeof score !== 'number') return new Response(JSON.stringify({ success: false }), { status: 400, headers: { 'Content-Type': 'application/json' } });
        const entry = { id: crypto.randomUUID(), name: String(name).slice(0, 20), score: Math.floor(score), brainSize, brainLabel, timestamp: Date.now() };
        let lb = []; try { lb = await env.LEADERBOARD.get('leaderboard_v1', { type: 'json' }) || []; } catch {}
        lb.push(entry); lb.sort((a, b) => b.score - a.score); lb = lb.slice(0, 500);
        await env.LEADERBOARD.put('leaderboard_v1', JSON.stringify(lb));
        return new Response(JSON.stringify({ success: true, entry, rank: lb.findIndex(e => e.id === entry.id) + 1 }), { headers: { 'Content-Type': 'application/json' } });
      } catch (e) { return new Response(JSON.stringify({ success: false, error: e.message }), { status: 500, headers: { 'Content-Type': 'application/json' } }); }
    }
    if (request.method === 'OPTIONS') return new Response(null, { headers: { 'Access-Control-Allow-Origin': '*', 'Access-Control-Allow-Methods': 'GET, POST, OPTIONS', 'Access-Control-Allow-Headers': 'Content-Type' } });
    return new Response(HTML, { headers: { 'Content-Type': 'text/html; charset=utf-8' } });
  }
};
